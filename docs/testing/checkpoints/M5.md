# M5 Prototype Checkpoint

| Field | Value |
|---|---|
| Status | Approved |
| Owner | Lead Agent / QA Steward |
| Last Verified | 2026-09-03 |
| Source of Truth For | M5 candidate, prototype path, verification, hard gates, known gaps and stop condition |
| Related Documents | `../TEST-STRATEGY.md`, `../ACCEPTANCE-TESTS.md`, `../../planning/ROADMAP.md`, `../../planning/BACKLOG.md`, `../../../artifacts/test-evidence/milestones/M5/manifest.json` |
| Milestone | M5 — Context and proactive scheduling |
| Candidate commit | `f33f417a8d34983f4682105380967301c8122503` |
| Baseline checkpoint | `59ca9e8fc4e0d6cee330bea6dfdd0786bac76b99` |
| Conclusion | `Passed with known gaps` |
| Target milestone | None — Product Owner directed this run to stop after M5; M6 is not active |
| Evidence manifest | [`artifacts/test-evidence/milestones/M5/manifest.json`](../../../artifacts/test-evidence/milestones/M5/manifest.json) |

## Environment

- Windows 11 Home x64, build `10.0.26200`, Australia/Sydney.
- Rust `1.98.0`, Node `24.19.0`, pnpm `11.16.0`.
- Product checks were hermetic except dependency advisory/index refresh and the local Windows desktop/WebDriver process boundary. No live Open-Meteo, OpenAI, user music, physical audio, device location, notification permission mutation, upload or external resource creation was used. Desktop runs used pinned `tauri-driver 2.0.6`, Microsoft EdgeDriver `152.0.4191.62` and isolated repository-ignored app-data roots.
- M3/M4 remained approved baselines. Their explicitly deferred non-hard M7 gaps were not reopened as M5 prerequisites.

## Completed scope

- `TASK-023` is `Done`: API-048 geocoding runs only after an explicit search, accepts a bounded query and returns Rust-owned in-memory candidates. API-049 saves coordinates only for an unexpired candidate explicitly selected by the user and uses revisioned persistence. Device location is absent.
- Forecast uses only the selected coordinates rounded to four decimals, the selected IANA timezone and fixed allowlisted weather parameters. Provider responses are bounded and validated; redirects, inconsistent timezone, non-finite values and malformed bodies fail closed. A 30-minute fresh cache may enter program Context; stale, missing, offline or failed weather is omitted and shown independently. Weather cache retention is seven days.
- `TASK-024` is `Done`: API-032–API-035, EVT-007, persistent weekly rules, transactional occurrence claims, IANA/DST calculation, 10/30/60-minute single snooze, 15-minute resume boundary and cancellable scheduler actor are integrated. Nonexistent local times advance to the first valid minute; ambiguous local times choose the earliest UTC occurrence.
- Native Windows notifications are silent and expose a fixed action allowlist for Start, Snooze 10/30/60, Dismiss and Open. Native callbacks reuse API-035 state transitions; only `Starting` invokes the radio starter, and API-024 consumes the start grant exactly once before planning, paid transport, speech or playback.
- Long sleep advances past missed occurrences instead of backfilling. Duplicate ticks, stale revisions, replayed actions and callbacks for deleted rules fail closed. Weak notification/authorization edges and actor `Drop` cancellation prevent strong cycles and orphaned scheduler tasks.
- Final security lifecycle review found and fixed a startup-order race: notification and program-start dependencies now bind before the scheduler actor begins evaluating due work.
- The first EdgeDriver session exposed a second startup defect: scheduler `start` called `tokio::spawn` from Tauri's synchronous setup without an entered Tokio reactor. The actor and its owned handle now use `tauri::async_runtime`; a synchronous-start regression test and both M5 desktop E2E cases pass.

## Executed checks

| Check | Result | Evidence summary |
|---|---|---|
| `cargo test --workspace --all-targets --locked` | Passed | 264 tests passed, 2 explicit live credential canaries ignored, 0 failures on the exact candidate |
| Focused weather Rust suite | Passed | 6/6: explicit search, signed selection, minimized forecast, response validation, fresh-context/no-refetch and seven-day retention |
| Focused scheduler Rust suite | Passed | 8/8: synchronous Tauri-runtime startup, CRUD/restart, DST, silent one-time start authority, resume, single snooze, native allowlist and native-start routing |
| `pnpm test -- schedule weather` | Passed | 4 files and 6 focused IPC/UI tests passed |
| `cargo fmt --all -- --check` | Passed | Full Rust workspace formatted |
| `cargo clippy --workspace --all-targets --all-features -- -D warnings` | Passed | Strict lint gate passed |
| `pnpm check` | Passed | Contract generation, lint, types, 98 frontend tests, 9 E2E harness self-tests, production build and documentation verification passed |
| `pnpm test:e2e -- weather` | Passed | A real Tauri/WebView2 desktop session reached the Context page and exposed manual city search without triggering provider work |
| `pnpm test:e2e -- schedule` | Passed | A real Tauri/WebView2 desktop session created and persisted one notification-only schedule rule while local-only onboarding stayed silent |
| Frontend coverage | Passed prototype baseline | Statements 74.80%, branches 69.84%, functions 71.03%, lines 77.10%; M7 beta thresholds remain deferred |
| `pnpm test:axe` / `pnpm test:visual` | Passed | 6 representative axe cases and 2 visual-policy cases passed |
| Contract generation | Passed | Digest `c54de738993a231bdf15fdf158ec8821e4a667fc716cce0902ef28e01c3f26ad`; generated Rust/TypeScript types remained synchronized |
| Dependency policy | Passed with visible warnings | `pnpm audit --prod` found no known vulnerability; `cargo deny check` passed; `cargo audit` found no vulnerability advisory and retained 18 policy-allowed maintenance/unsound warnings |
| Static boundary scan | Passed | Exact source diff reverified: 0 secret-shaped assignments, 0 absolute user paths, 0 new unsafe log calls and 0 frontend `fetch` calls |
| Security diff scan | Passed | Exact `59ca9e8fc4e0d6cee330bea6dfdd0786bac76b99..f33f417a8d34983f4682105380967301c8122503` range; complete 24/24 inventory, 8 attack surfaces, zero findings at every severity |

## Acceptance inventory

| Item | Status | Interpretation |
|---|---|---|
| `TEST-WEA-001` | Passed with known gaps | Rust and React tests prove explicit search, same-name candidate selection, revisioned persistence and attribution; the real desktop settings smoke passes, while live Open-Meteo remains `Not Run` |
| `TEST-WEA-002` | Passed with known gaps | Captured fake transport proves minimized forecast, observation time, fresh-cache context and stale/failure omission; live offline desktop timing is `Not Run` |
| `TEST-SCH-001` | Passed with known gaps | CRUD/restart, a real desktop notification-only rule save, and deterministic ordinary/nonexistent/ambiguous IANA times pass; the full real-clock DST/repeat matrix is `Not Run` |
| `TEST-SCH-002` | Passed with known gaps | Counting fakes prove notification/due/resume are zero sound and zero paid call until one explicit Start; real Windows permission/render/buttons/silence observation is `waiting for user` |
| `TEST-SCH-003` | Passed | 10 → 30 replacement leaves one snooze, 60-minute snooze passes, and the weekly rule remains unchanged |
| `TEST-PRIV-001` M5 subset | Passed | Static capability/dependency/outbound review proves no device-location or frontend-network capability; weather sends only contract-approved fields |
| `TEST-SEC-001` M5 subset | Passed | Secret/path/log/static network checks and exact-candidate security review found no leak or new unauthorized boundary |
| `TEST-SEC-003` | Passed with known warnings | No known dependency vulnerability and deny policy passes; 18 allowed maintenance/unsound warnings remain visible |
| `TEST-MAINT-001` M5 subset | Passed | Strict contracts, types, lint, build and documentation verification pass |
| `TEST-A11Y-001` M5 subset | Passed with known gaps | Settings weather/schedule controls are component-tested and representative axe cases pass; Narrator/full-resolution matrix is `Not Run` |
| `TEST-MAINT-002` | Not Run | Full M7 beta coverage thresholds were not enforced at this prototype checkpoint; measured coverage is recorded above |

## Hard gates

| Gate | Result | Evidence |
|---|---|---|
| Secret boundary | Passed | Weather and scheduling add no credential API; exact diff contains no secret-shaped assignment and no raw provider body reaches WebView, IPC errors or logs |
| File/network/permission scope | Passed | No device location, frontend network, new filesystem scope, microphone or screen capability; Open-Meteo endpoints and request fields are Rust-owned fixed allowlists |
| Logging redaction | Passed | Provider, schedule and notification failures expose stable reasons only; static scan found no new log calls or absolute user paths |
| Explicit sound and paid action | Passed | Notification delivery, due evaluation, recovery, snooze, dismiss, open and unknown actions cannot start; one explicit Start creates a grant consumed once before all side effects |
| Public contract validity | Passed | API-032–API-035, API-048/API-049 and EVT-007 agree across API-CONTRACT, provider contract, schemas, Rust DTOs and TypeScript validators |
| Data integrity and retention | Passed | Schedule/rule/occurrence/location/cache writes are transactional, revisioned, bounded, constraint-backed and restart tested; long sleep does not backfill |
| Critical/High security findings | Passed | Final exact immutable-candidate scan found zero Critical, High, Medium or Low findings; dependency policies contain no unallowed vulnerability |

No hard gate is `Failed` or `Not Run`.

## Actual failures and recovered attempts

- Weather integration patching initially missed the current `lib.rs` context. This mechanical write attempt made no partial change; smaller scoped patches applied cleanly.
- The first weather request-minimization test falsely treated the legitimate timezone value `Australia/Sydney` as a leaked city label. The assertion was narrowed to forbidden query keys; all 6 weather tests passed.
- One combined `cargo test` command supplied a second filter argument unsupported by the test harness. No test ran; the focused suites were split and passed independently.
- Early PowerShell/`rg` inspections used an unsupported glob, the old `docs/TRACEABILITY.md` path and later the nonexistent `src-tauri/src/weather.rs` path. These read-only errors were corrected to explicit paths and `src-tauri/src/weather/mod.rs`.
- Scheduler compile attempt 1/3 exposed an unformatted test module, an unexported `RadioFuture` and unused imports. Exports/imports and formatting were corrected; focused and full builds passed.
- Frontend typecheck attempt 1/3 rejected a generic `PublicEventPayload` cast. The boundary now converts through `unknown`; typecheck passed.
- Initial scheduler formatting checks found mechanical line wrapping. `cargo fmt` corrected it and the workspace formatting gate passed.
- Strict Clippy attempt 1/3 reported eight issues; attempt 2/3 retained one borrowed test-call issue; targeted fixes produced attempt 3/3 pass.
- Desktop E2E was initially classified `waiting for user` because no host-visible matching `msedgedriver.exe` was available. After the user supplied EdgeDriver `152.0.4191.62`, its version, hash and host visibility were verified and that prerequisite closed.
- The first Codex-hosted desktop attempt then hit the E2E cleanup reparse guard because AppContainer path virtualization made the lexical and resolved application-data roots differ. The exact dedicated test directories were safely renamed rather than recursively deleted, and later runs used repository-ignored isolated roots.
- Both first host-visible M5 sessions still returned the generic `desktop_session_failed`. A bounded raw W3C diagnostic showed EdgeDriver was healthy and captured the actual application panic at `schedule/mod.rs:608`: `tokio::spawn` ran without a Tokio reactor during synchronous Tauri setup.
- The scheduler actor/handle moved to `tauri::async_runtime`; the new no-Tokio-context regression test, scheduler 8/8 suite and weather/schedule desktop E2E all passed. No provider, notification permission, paid call or audio side effect was used by those desktop smokes.
- An early `cargo audit` refresh encountered a transient yanked-registry status timeout while returning a non-failing result. The final exact-candidate audit refreshed cleanly, found no vulnerability and reported 18 allowed warnings.
- Several larger scheduler/native `apply_patch` hunks missed moving contexts. They produced no partial edits; smaller single-owner patches applied and were verified.
- The first final-scan launch used a guessed, invalid full SHA and was rejected before scan state existed. `git rev-parse` established the exact candidate.
- A scan of the preceding candidate was canceled after review exposed the startup binding race; the fix was committed and a new immutable scan became the sole final security result.
- Native-action formatting attempt 1/3 and 2/3 found only wrapping differences; attempt 3/3 passed after formatting.
- Native-action Clippy attempt 1/3 found a needless return, elidable lifetimes and a test guard held across `await`; the minimal fixes made attempt 2/3 pass.
- A superseded security draft submission omitted the required exclusion `pattern`; the corrected draft was accepted before that superseded scan was abandoned.
- Final security preflight progress synchronization was rejected three times: derived fields were supplied, the scan phase had already advanced, then backward phase transition was forbidden. Calls stopped at 3/3; the independent preflight and architecture results remain recorded in this checkpoint and final coverage, while the final scan itself completed successfully.
- The first final security draft used obsolete optional coverage fields and surface names. The returned schema errors were applied; the second draft was accepted and the scan sealed successfully.

## Not Run and known gaps

| Gap | Status | Impact | Mitigation | Target |
|---|---|---|---|---|
| Real Windows notification permission, rendering, buttons and audible-silence observation | `Not Run` / `waiting for user` | Native OS presentation is not manually witnessed | Pinned native adapter emits silent toast XML; exact allowlist and start routing have hermetic tests; no permission was changed | M7 |
| Full DST, sleep/resume, repeat, multi-notification and offline recovery matrix | `Not Run` | Real clock/device combinations are not release-qualified | Deterministic DST, single-snooze, duplicate tick and 15-minute resume tests cover M5 hard properties | M7 |
| Live Open-Meteo, OpenAI and physical audio | `Not Run` | Production network timing and real audible behavior are not claimed | Bounded fakes capture exact requests and side-effect counts; default tests remain hermetic | M7 |
| Delivered toast handle is not retained for proactive removal after rule deletion | Known gap | A deleted rule's toast may remain visually stale until Windows dismisses it | Cascading persistence deletion makes every later callback fail closed and grants no start authority | M7 |
| Expired geocoding candidate records are lazily purged from process memory | Known gap | An unusable candidate may occupy bounded memory until another weather operation or exit | Ten-minute authority expiry is enforced on access and candidates are never persisted | M7 privacy hardening |
| Narrator, complete keyboard/state/resolution/scaling and coverage-threshold matrices | `Not Run` | Full assistive-technology and beta coverage claims are deferred | Representative axe, component keyboard, visual-policy and measured coverage evidence pass the prototype gate | M7 |
| `cargo audit` retains 18 policy-allowed transitive maintenance/unsound warnings | Known warning | Future ecosystem support may need dependency stewardship | `cargo deny check` and vulnerability audit pass; no unallowed Critical/High advisory is present | M7 dependency stewardship |
| M3/M4 checkpoint known gaps remain open | Unchanged | M5 does not claim to close prior physical, live-provider, long-run or full-matrix gaps | Their checkpoint mitigations remain in force and were explicitly not M5 prerequisites | M7 |

## Security evidence

- Scan ID: `f870c7a2-746d-43a6-ab6c-c60de8041d76`.
- Final report: 33,980 bytes; SHA-256 `318c595f00de778a4b784e7a96c434194aad76b03afa54e29073f1aab0911841`.
- Complete usage: 6,325,230 total tokens; 6,297,231 input, 6,032,384 cached input, 27,999 output, 13,609 reasoning output; three scan threads.
- TAC advisory status could not be confirmed because the optional connector was not signed in. TAC is advisory and did not gate the local scan.

## Conclusion and stop

M5 is `Passed with known gaps`: the candidate proves explicit city search/selection → minimized fresh weather Context and persistent weekly rule → silent notification → explicit one-time Start, and now also passes the real Windows Tauri/WebView2 weather and schedule desktop smokes. Stale weather, offline failure, long sleep, ignored/dismissed notifications and replayed actions remain silent and make zero paid calls. All always-applicable hard gates passed; recovered failures and non-hard deferred checks remain explicit above. Per the Product Owner stop condition, M5 is closed here, M6 remains inactive and unclaimed, and no push, publication, upload, artifact release or external resource creation occurred.
