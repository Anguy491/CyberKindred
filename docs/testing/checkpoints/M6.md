# M6 Prototype Checkpoint

| Field | Value |
|---|---|
| Status | Approved |
| Owner | Lead Agent / QA Steward |
| Last Verified | 2026-09-06 |
| Source of Truth For | M6 candidate, prototype path, verification, hard gates, known gaps and transition |
| Related Documents | `../TEST-STRATEGY.md`, `../ACCEPTANCE-TESTS.md`, `../../planning/ROADMAP.md`, `../../planning/BACKLOG.md`, `../../../artifacts/test-evidence/milestones/M6/manifest.json` |
| Milestone | M6 — Apple Music companion mode |
| Candidate commit | `ea2f2fcc4d5ff077291119936aa975b37e84fe75` |
| Baseline checkpoint | `33c8c06b981c67b7dac9d17588431ad48a075ef3` |
| Conclusion | `Passed with known gaps` |
| Target milestone | M7 is Active; beta/release approval remains with the Product Owner |
| Evidence manifest | [`artifacts/test-evidence/milestones/M6/manifest.json`](../../../artifacts/test-evidence/milestones/M6/manifest.json) |

## Environment

- Windows 11 Home x64, build `10.0.26200`, Australia/Sydney.
- Apple Music Windows App `1.1540.23042.0`, x64, with a user-prepared playing session.
- Rust `1.98.0`, Node `24.19.0`, pnpm `11.16.0`.
- The live check used only local Windows GSMTC and one user-authorized pause/restore cycle. No Apple account identifier, track text, artwork, path, credential or session token was recorded; no paid provider or external network was invoked.
- Default automated checks remained hermetic. The live Apple test is ignored unless `CYBERKINDRED_LIVE_APPLE_MUSIC=1` is explicitly set.

## Implemented scope

- `TASK-025` is `Done`: the Apple Music Windows App GSMTC source supports explicit discovery, installation/session states, bounded observed fields, truthful runtime capabilities, playback state/revision mapping, failure refresh and session replacement/loss handling. Selecting Local or shutting down now awaits source deactivation and revokes observation.
- `TASK-026` is `Done`: Apple reactions are local deterministic visible text, never a claimed exact queue. Apple/GSMTC data is structurally absent from Responses and Speech inputs. Generic-only TTS uses session, revision and activation generation so source changes, user override or session replacement cannot resume stale playback.
- `TASK-027` is `Done`: the 19-class inventory, versioned export, five exact category deletes and full reset are integrated. Export staging is lifecycle-fenced and crash-purged; profile/location/trends, conversation hashes and late weather writes obey deletion ownership; V0004 detaches only terminal local history tombstones when Library Index is deleted; full reset closes admission and awaits all sound/network/write producers before credential/storage/file cleanup.
- `TASK-028` is `Done`: Settings, masked provider credentials, Apple status, tray and per-user autostart are integrated; startup and tray paths remain silent.

## Executed checks

| Check | Result | Evidence summary |
|---|---|---|
| `pnpm check` | Passed | Contract generation, lint, strict types, 19 files / 109 frontend tests, coverage, E2E harness self-tests, production build and documentation verification passed |
| `cargo fmt --all -- --check` | Passed | Rust formatting is clean |
| `cargo clippy --workspace --all-targets --all-features -- -D warnings` | Passed | Strict Rust lint gate passed |
| `cargo test --workspace --all-features` | Passed | 301 tests passed across workspace binaries/integration suites, 3 explicit live canaries ignored, 0 failed; main library binary: 276 passed / 2 ignored |
| Focused Apple lifecycle suites | Passed | Explicit discovery, capability/state mapping, session loss/replacement, source deactivation, stale resume revocation, user override and Apple-data provider boundary pass |
| Live product-service Apple test | Passed | Production GSMTC adapter connected to a real Apple App track, observed Playing + Pause capability, paused for a generic interruption and restored the unchanged session to Playing |
| Security remediation review | Passed | Sealed 6 Medium/3 Low finding families fixed; a fresh independent bypass review found one High reset-admission race, which was then closed by atomic gates and actual task-completion waits; zero open Critical/High |
| `git diff --check` | Passed | No whitespace error; only configured Windows LF-to-CRLF notices |

## Acceptance inventory

| Item | Status | Interpretation |
|---|---|---|
| `TEST-APL-001` | Passed with known gaps | M1 evidence covers App absent, Web-only, live App and disappearance at probe level; the M6 product service connected to a real Apple App session. Repeating every absence/replacement state on this candidate remains M7 evidence. |
| `TEST-APL-002` | Passed with known gaps | A real track/status/capability and actual pause/restore were observed without recording media text. The 50-iteration and cross-version convergence matrix remains M7. |
| `TEST-APL-003` | Passed with known gaps | Static/provider-boundary and deterministic-template tests prove zero Apple metadata in Responses/Speech. A live proxy capture is retained for M7 because this checkpoint made no provider call. |
| `TEST-APL-004` | Passed with known gaps | Real generic interruption pause/restore passed; hermetic tests cover user override, source switch, session replacement/loss and stale token revocation. Live user-override, sleep and device-switch repetitions remain M7. |
| `TEST-DAT-001` | Passed | The path-free 19-class inventory and Settings presentation tests pass. |
| `TEST-DAT-002` | Passed | Versioned export allowlist and cleanup canaries pass; previous-crash staging is purged and a cleanup failure cannot report completion. |
| `TEST-DAT-003` | Passed | Exact category ownership, profile/location/trend removal, conversation-source removal, weather late-write fencing, Library Index tombstones, DB integrity and source-music non-deletion pass. |
| `TEST-DAT-004` | Passed with known gaps | Automated credential/path/producer-quiescence/reset-scope gates pass. A destructive reset of the user's real profile plus native restart-empty-state observation was intentionally not performed and remains M7. |
| `TEST-SET-001/002/003` M6 subset | Passed | Settings, masked keys, Apple connection action, tray/autostart transitions and silent startup paths pass. |

## Hard gates

| Gate | Result | Evidence |
|---|---|---|
| Secret boundary | Passed | Credentials remain Rust/Credential-Manager-only; Apple data remains local; canary/export/static tests show no secret crossing. |
| File/network/permission scope | Passed | No browser DOM automation, MusicKit, frontend network, filesystem expansion, microphone or screen permission was introduced. |
| Logging redaction | Passed | Public errors/events remain bounded and path-free; live evidence contains only structural field/capability presence. |
| Explicit sound and paid action | Passed | The user explicitly prepared the live session and authorized the check. One local GSMTC pause/restore occurred; no paid provider call was made. Unconfirmed paths remain zero-sound/zero-paid-call in hermetic tests. |
| Public contract validity | Passed | Generated Rust/TypeScript schemas and API/EVT contracts remain synchronized; `pnpm check` and Rust contract suites pass. |
| Data integrity | Passed | V0004 forward migration and terminal tombstone constraints preserve retained history while allowing exact Library Index deletion; migration, delete and integrity tests pass. |
| Privacy deletion/reset lifecycle | Passed | Category lifecycle fences and full-reset atomic admission/quiescence prevent covered data or producers from surviving/recreating state before success. |
| Critical/High security findings | Passed | The independent review's one High admission race was fixed; there are zero open Critical/High findings. |

## Security evidence

- Original sealed scan ID `628d9567-ff70-4332-ab6a-5ff8e23a41cc` covered exact range `33c8c06b981c67b7dac9d17588431ad48a075ef3..9c3add0853082c8578d7de490904a94e58e69fc9`, 45/45 files, and reported 6 Medium/3 Low with zero Critical/High. Report SHA-256: `58f493e10311e5469ccd6f3cc9db05ac5d0d824dbf88665cff2339c230d808d0`.
- Candidate `ea2f2fcc4d5ff077291119936aa975b37e84fe75` remediates all nine reported families: Apple observation revocation, stale source-switch resume, profile/location/trend residue, conversation source hashes, late weather writes, Library Index rollback, export staging retention and full-reset quiescence.
- The required fresh read-only bypass/regression review found one additional High: reset admission checks were not atomic with background-operation registration. The candidate now serializes admission with reset in radio, scanner, understanding and provider preview, closes admission before snapshot, cancels accepted work and awaits task completion. Parent inspection, strict clippy and the full Rust suite confirm the fix; no open Critical/High remains.
- `RISK-022` retains three unproven platform hypotheses—AUMID publication authority, adversarial artwork decoder amplification and a concrete unauthorized WebView invocation path for API-037—for M7 VM/adversarial validation. They are not verified vulnerabilities and current strict boundaries remain in force.

## Known gaps

| Gap | Impact | Mitigation | Target |
|---|---|---|---|
| Cross-version/repeated Apple matrix | A single live product run does not prove 50-iteration timing or every Apple/Windows version, user override, disappearance, sleep and device transition. | Runtime capabilities fail closed; identity/revision/generation checks and hermetic race tests cover the safety invariant. | M7 |
| Destructive native reset/restart observation | The user's real profile was not erased merely to prove the UI returns to onboarding. | Automated temporary-root tests prove exact targets, quiescence, credentials/path scope and preservation of external music/export; M7 runs the destructive flow in an isolated install/VM. | M7 |
| Platform adversarial proof | `RISK-022` platform hypotheses remain unresolved. | Keep strict AUMID, image and Tauri command boundaries; do not expand protocols or renderer authority before VM/adversarial evidence. | M7 |

## Conclusion

M6 is `Passed with known gaps`. Its milestone-defined real Apple path, explicit action gate, contract boundary, data integrity, privacy lifecycle and security hard gates pass on candidate `ea2f2fcc4d5ff077291119936aa975b37e84fe75`; there are no open Critical/High findings. The remaining evidence is the release-grade matrix explicitly permitted to move to M7. `TASK-025`–`TASK-028` are closed, M7 is automatically Active, and no push, release, upload or external publication occurred.
